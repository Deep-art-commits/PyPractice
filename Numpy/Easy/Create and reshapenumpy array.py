# Create a 1D array of numbers from 0 to 9.
import numpy as np
# n,u=[int(i) for i in input("Enter Upper and lower bound for generating array: ").split(",")]
# arr=np.arange(n,u)
# print(arr)
# r,c=[int(i) for i in input("Enter rows  and colums for reshaping array: ").split(",")]
# print(arr.reshape(r,c))


# Find the sum of all elements in the array [1, 2, 3, 4, 5].
arr= [1, 2, 3, 4, 5]
ar=np.array(arr)
print(ar.sum())